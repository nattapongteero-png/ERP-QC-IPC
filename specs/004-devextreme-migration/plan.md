# Implementation Plan: DevExtreme UI Migration

**Branch**: `004-devextreme-migration` | **Date**: 2025-12-19 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/004-devextreme-migration/spec.md`

## Summary

Migrate all 30 existing UI components from custom Radix UI/Tailwind-based implementation to DevExtreme by DevExpress, then **completely remove** Tailwind CSS, Radix UI, and shadcn/ui from the project. This enables enterprise-grade features including advanced data grids with sorting/filtering/export, rich form controls with validation, and consistent theming. DevExtreme will handle ALL styling including layout and spacing (no external CSS frameworks).

## Technical Context

**Language/Version**: TypeScript 5.x with React 19, Next.js 15 (App Router)
**Primary Dependencies**: DevExtreme React 25.1.x, devextreme-themebuilder 25.1.x, Zod 4.2.x (existing)
**Storage**: N/A (no database changes - UI-only migration)
**Testing**: Vitest for unit tests, Playwright for E2E (existing)
**Target Platform**: Web (desktop, tablet, mobile responsive)
**Project Type**: Web application (Next.js App Router)
**Performance Goals**: Page load < 3 seconds, API response < 500ms, table operations < 500ms for 1000 rows
**Constraints**: Bundle size increase must be justified, maintain WCAG 2.1 AA accessibility
**Scale/Scope**: 30 components, 11 pages, 6 component categories

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Requirement | Status | Notes |
|-----------|-------------|--------|-------|
| I. Code Quality | TypeScript strict mode, no `any` types | PASS | DevExtreme has full TypeScript support |
| I. Code Quality | No hardcoded values | PASS | Theme via config, locale via external file |
| I. Code Quality | Error Verification | PASS | Will run `pnpm tsc --noEmit` and `pnpm lint` after each task |
| I. Code Quality | Frequent Commits | PASS | Will commit after each component migration |
| II. Testing | Unit tests for primary paths | PASS | Will update tests for migrated components |
| II. Testing | Integration tests for APIs | N/A | No API changes in this migration |
| III. UX | Responsive design (desktop/tablet/mobile) | PASS | DevExtreme supports responsive layouts |
| III. UX | Loading states | PASS | Will use DevExtreme LoadIndicator + existing skeletons |
| III. UX | Error feedback in user's language | PASS | Thai locale via custom dictionary |
| III. UX | Form validation inline | PASS | DevExtreme Validator component + Zod adapter |
| III. UX | Consistent styling | PASS | DevExtreme-only styling with custom emerald theme; Tailwind will be removed |
| III. UX | Accessibility | PASS | DevExtreme is WCAG 2.1 compliant |
| IV. Performance | Page load < 3 seconds | WATCH | Bundle size increase requires lazy loading |
| IV. Performance | Bundle size < 500KB gzipped | VIOLATION | DevExtreme adds ~1-3MB; requires justification |
| IV. Performance | N+1 queries prohibited | N/A | No database changes |
| V. Security | Authentication required | N/A | No auth changes |
| V. Security | Input validation | PASS | Zod schemas maintained via adapter |

### Constitution Violations Requiring Justification

| Violation | Justification | Mitigation |
|-----------|--------------|------------|
| Bundle size > 500KB | DevExtreme provides enterprise features (data grid, export, filtering) not available in smaller libraries. These features are critical for ERP workflows. | 1. Use module-specific imports for tree-shaking<br>2. Lazy load heavy components (DataGrid, DateBox)<br>3. Measure and document actual bundle impact<br>4. Only load DevExtreme on pages that need it |

## Project Structure

### Documentation (this feature)

```text
specs/004-devextreme-migration/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Research findings
├── data-model.md        # N/A (no data model changes)
├── quickstart.md        # Component migration guide
├── contracts/           # N/A (no API contracts)
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Implementation tasks (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
src/
├── components/
│   ├── ui/                          # DevExtreme component wrappers
│   │   ├── dx-data-grid.tsx         # DataGrid wrapper
│   │   ├── dx-text-box.tsx          # TextBox wrapper
│   │   ├── dx-select-box.tsx        # SelectBox wrapper
│   │   ├── dx-date-box.tsx          # DateBox wrapper
│   │   ├── dx-button.tsx            # Button wrapper
│   │   ├── dx-popup.tsx             # Popup wrapper
│   │   ├── dx-form.tsx              # Form wrapper
│   │   └── index.ts                 # Re-exports
│   ├── layout/                      # Keep existing (minimal DevExtreme use)
│   │   ├── main-layout.tsx
│   │   └── sidebar.tsx
│   └── providers/
│       ├── providers.tsx            # Updated with DevExtreme
│       └── devextreme-provider.tsx  # NEW: DevExtreme setup
├── lib/
│   └── validation/
│       └── zod-devextreme-adapter.ts # NEW: Zod integration
├── localization/
│   └── th.json                      # NEW: Thai translations
└── app/
    └── globals.css                  # Updated: DevExtreme theme import

devextreme-theme/
└── emerald-metadata.json            # NEW: Theme configuration

public/
└── css/
    └── dx.material.emerald.css      # GENERATED: Custom theme
```

**Structure Decision**: Maintain existing Next.js App Router structure. Replace all UI components with DevExtreme equivalents. After migration, remove `tailwind.config.ts`, `postcss.config.js`, and all Tailwind/Radix dependencies. Layout components (sidebar, main-layout) will use DevExtreme Drawer and ResponsiveBox for responsive behavior.

**Files to Remove After Migration:**
- `tailwind.config.ts`
- `postcss.config.js`
- All old component files in `src/components/ui/` (replaced by DevExtreme wrappers)
- `src/app/globals.css` Tailwind directives (keep only DevExtreme theme import)

## Migration Phases

### Phase 1: Foundation Setup

1. Install DevExtreme dependencies
2. Configure emerald theme with ThemeBuilder
3. Create DevExtreme provider with Thai locale
4. Create Zod-DevExtreme validation adapter
5. Update globals.css with theme import
6. Verify build and lint pass

### Phase 2: Core Components (P1 Priority)

1. Migrate Table → DataGrid (data-grid.tsx)
2. Migrate Input → TextBox (input.tsx)
3. Migrate Select → SelectBox (select.tsx)
4. Migrate DatePicker → DateBox (date-picker.tsx)
5. Migrate Button → Button (button.tsx)
6. Update item and warehouse pages to use new components

### Phase 3: Dialog & Form Components (P2 Priority)

1. Migrate Dialog → Popup (dialog.tsx)
2. Migrate ItemSearchDialog → DevExtreme Popup with DataGrid
3. Migrate CustomerSearchDialog → DevExtreme Popup with DataGrid
4. Migrate ItemEditForm → DevExtreme Form
5. Migrate WarehouseEditForm → DevExtreme Form
6. Create form-field wrapper for consistent validation display

### Phase 4: Supporting Components (P3 Priority)

1. Migrate DropdownMenu → DevExtreme ContextMenu/DropDownButton
2. Migrate Tabs → DevExtreme TabPanel
3. Migrate Tooltip → DevExtreme Tooltip
4. Update Card, KPI Card, Stat Card with consistent styling
5. Update Empty State, Skeleton components
6. Migrate Badge with DevExtreme styling patterns

### Phase 5: Layout Components Migration

1. Migrate Sidebar → DevExtreme Drawer component
2. Migrate MainLayout → DevExtreme ResponsiveBox + Drawer
3. Migrate PageHeader → DevExtreme Toolbar
4. Replace all Tailwind utility classes with DevExtreme CSS classes
5. Update responsive breakpoints to use DevExtreme media queries
6. Verify navigation and responsive behavior

### Phase 6: Legacy Cleanup

1. Remove all Tailwind CSS classes from remaining files
2. Remove all `@radix-ui/*` imports and replace with DevExtreme
3. Uninstall deprecated packages:
   - `tailwindcss`, `@tailwindcss/*`
   - `@radix-ui/*`
   - `class-variance-authority`
   - `clsx`, `tailwind-merge`
4. Delete configuration files:
   - `tailwind.config.ts`
   - `postcss.config.js`
5. Clean up `globals.css` (remove Tailwind directives)
6. Delete old component files that have been replaced
7. Verify zero Tailwind/Radix references remain (grep verification)
8. Update package.json and run `npm install` to clean lock file

### Phase 7: Integration & Testing

1. Run full E2E test suite
2. Performance audit (Lighthouse, bundle analysis)
3. Accessibility audit (axe-core)
4. Visual regression testing
5. Fix any remaining issues
6. Update component documentation
7. Final verification: grep for Tailwind/Radix references

## Component Mapping

| Current Component | DevExtreme Replacement | Priority |
|------------------|----------------------|----------|
| Table | DataGrid | P1 |
| Input | TextBox | P1 |
| Select | SelectBox | P1 |
| DatePicker | DateBox | P1 |
| Button | Button | P1 |
| Dialog | Popup | P2 |
| ItemSearchDialog | Popup + DataGrid | P2 |
| CustomerSearchDialog | Popup + DataGrid | P2 |
| ItemEditForm | Form | P2 |
| WarehouseEditForm | Form | P2 |
| DropdownMenu | DropDownButton | P3 |
| Tabs | TabPanel | P3 |
| Tooltip | Tooltip | P3 |
| Badge | Custom (styled) | P3 |
| Card, KpiCard, StatCard | Custom (styled) | P3 |
| Skeleton | LoadIndicator + Custom | P3 |
| EmptyState | Custom (styled) | P3 |
| Sidebar | Drawer | P5 |
| MainLayout | ResponsiveBox + Drawer | P5 |
| PageHeader | Toolbar | P5 |

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| Bundle > 500KB | Enterprise data grid features (virtual scrolling, export, column filters, sorting) are requirements FR-001, FR-014, FR-015 | ag-Grid is more expensive, MUI DataGrid lacks features, custom implementation would take months |

## Next Steps

1. Run `/speckit.tasks` to generate detailed implementation tasks
2. Each task will include specific acceptance criteria and test requirements
3. Tasks will be ordered by priority and dependency
